---
- name: Generate test certificates
  script: "scripts/create-fake-certificate.sh {{ acme.webroot }} {{ ssl.certificates_dir }} {{ owner.cert_mail }} {{ item }}"
  args:
    creates: "{{ ssl.certificates_dir }}/{{ item }}/privkey.pem"
  become: yes
  become_user: certbot
  with_items: "{{ domains }}"
